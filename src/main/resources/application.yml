server:
  port: 8078

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/netclient
    username: masterpiece
    password: zntPEGASUS77K
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

api:
  version: v1